# Halim guardrails (frontier)

When `halim/` is a separate repo, copy `models/halim_constitution.json` here as `CONSTITUTION.json`.

Runtime enforcement currently lives in the HANOON tradingbot:

`core/halim_guardrails.py`

See tradingbot `docs/HALIM_GUARDRAILS.md` for full policy.
