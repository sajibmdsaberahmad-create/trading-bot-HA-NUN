---
description: Life Engine vision — one ship, AI everywhere, RAM-live, maturity ladder
alwaysApply: true
---

# HANOON Life Engine (read before any trading-loop change)

Full spec: `docs/VISION_SMART_STACK.md` · Hub: `core/smart_stack.py`

## Mission

**One powerful ship, not scattered boats.** Smart sensors → PPO reflex → Halim reason → blend → sampled teacher → war posture → HARD survival rails → learn every spike.

Entire operation uses AI for better results. Mechanical gates/war/rules **exist** but feed AI as context/posture — never bypass brains silently.

## Now vs maturity

Foundation is wired today; quality improves via `brain_maturity` stages (newborn→adult) + gold data:
- **Now:** Halim+PPO lead, advisory gates, verdict logging, teacher curriculum, war posture, RAM-live
- **Matures:** Halim adult entries, PPO beyond HOLD, calibrated thresholds, zero-API sessions, war-per-regime

See `maturity_ladder()` in `core/smart_stack.py`.

## Never do (SMART_STACK=true default)

- `ppo_hold_skip` before Halim/council
- Hard-block gates in spike loop without `decide_entry`
- Disk cleanup/sweeps during market hours (`RAM_LIVE_ONLY=true`)
- Teacher API on every ticker every spike
- New standalone entry bot outside `ai_commander` pipeline
- Skip `_emit_spike_verdict` on any finalize path

## RAM-live

- `RAM_LIVE_ONLY=true` (default with smart stack): no periodic cleanup while market open
- Off-hours: tail-trim jsonl with bounded RAM (deque), not full-file load
- One hull: `scalper_runner` — extend organs, don't spawn parallel decision systems

## Touch together

`core/smart_stack.py`, `core/ai_commander.py`, `core/scalper_runner.py`, `core/entry_quality.py`, `core/brain_maturity.py`
